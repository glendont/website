import React, { Fragment } from "react";
import SearchProjects from "./SearchProjects";

const ProjectPage = () => {
  return (
    <Fragment>
      <SearchProjects />
    </Fragment>
  );
};

export default ProjectPage;
